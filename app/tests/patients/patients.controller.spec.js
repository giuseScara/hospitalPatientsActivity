describe("Controller Test", function () {
  var mockScope = {};
  var controller;

  beforeEach(module("AppModule"));

  /*beforeEach(angular.mock.module.inject(function ($controller, $rootScope) {
    mockScope = $rootScope.$new();

    controller = $controller("PatientsController", {
      //$scope = mockScope;
    });
  }));*/



});